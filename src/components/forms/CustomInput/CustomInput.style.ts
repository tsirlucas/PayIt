import {StyleSheet} from 'react-native';
import {colors} from 'style';

export const style = StyleSheet.create({
  labelError: {
    color: colors.danger,
  },
  message: {
    color: colors.danger,
    marginLeft: 15,
    marginTop: 5,
  },
});
