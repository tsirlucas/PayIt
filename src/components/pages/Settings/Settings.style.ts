import {StyleSheet} from 'react-native';

export const style: {[index: string]: Object} = StyleSheet.create({
  listSeparator: {
    fontSize: 14,
    marginTop: 20,
    marginBottom: 20,
    marginLeft: 20,
    color: 'gray',
  },
  list: {
    marginBottom: 15,
  },
});
